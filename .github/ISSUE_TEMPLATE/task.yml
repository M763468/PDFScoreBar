name: "Task / 雑務・リファクタ・ドキュメント"
description: "小さめの作業や整備"
title: "[Task] <短い要約>"
labels:
  - "task"
  - "priority:med"
  - "scope:small"
body:
  - type: markdown
    attributes:
      value: |
        > [!TIP]
        > AIエージェントにIssueの下書きを依頼する場合は `issue-creation` を利用できます。実装はこのIssueを直接指定して依頼してください。専用の実装skillは不要です。

  - type: markdown
    attributes:
      value: |
        ## ブランチ運用
        AIエージェントが作業するブランチを指定します。未定なら空欄でもOKで、あとからコメント等で指定できます。

  - type: input
    id: base_branch
    attributes:
      label: "Base branch"
      description: "作業ブランチの元となる親ブランチ名を指定します。"
      placeholder: "例: develop"

  - type: input
    id: branch_name
    attributes:
      label: "Branch name"
      description: "AIが作成する作業ブランチ名を指定します。"
      placeholder: "例: chore/123-update-docs"

  - type: input
    id: pr_base
    attributes:
      label: "PR base"
      description: "PRの向き先となるブランチ（通常はdevelop）"
      placeholder: "例: develop"

  - type: textarea
    id: goal
    attributes:
      label: "Goal"
      placeholder: "例: README に使い方を追記する"
    validations:
      required: true

  - type: textarea
    id: done
    attributes:
      label: "Done"
      description: "完了判定を箇条書きで。結果を見た後に別の基準へ読み替えない。"
      placeholder: |
        - README に CLI 例が追加されている
    validations:
      required: true

  - type: textarea
    id: notes
    attributes:
      label: "Notes"
      description: "注意点や関連リンクなど。"
    validations:
      required: false
