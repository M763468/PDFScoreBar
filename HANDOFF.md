# Session Handoff (Epic #120 Rebuild Roadmap)

## 1. 統合ブランチとマージの全体方針 (Roadmap)
Epic #120（90a278cを起点とした高精度パイプラインの再構築）は、すべてのスモールステップPRを段階的に **`rebuild/issue120`** ブランチ（統合ブランチ）に集約していく方針で進めています。

全体のロードマップは以下の通りです。
*   **Step 1 (完了 / PR #127)**: 前提となるCNNスケーリングバグ(`max_h`)の修正と、今後の精度維持のGateとなるGolden Baselineデータ・検証スクリプトの追加。
*   **Step 2 (完了 / PR #128)**: Phase 1 後半（In-process化、Batch Orchestrator、Docker/Env統合）。
*   **Step 3 (完了 / PR #129)**: Phase 3（ネイティブフィルタリングの実装とBox Tightening）。
*   **Step 4 (完了 / PR #131)**: Phase 2 前半（丸め誤差、Tall Band対策などのサイレントバグ・ロジック修正）。
*   **Step 5 (完了 / 未マージ)**: Phase 2 後半（PDFページ指定やimport整理などの純粋なリファクタリング・機能改善）。
*   **Final**: すべてのステップが `rebuild/issue120` に統合され、精度 `TP=3580, FP=0, FN=1` が完全に維持されていることを確認した上で、最終的に `develop` または `main` へ一括マージして Epic #120 を完了とします。

## 2. 現在の状況と検証スコープ

### これまでにテストした方法

1. **推論キャッシュを無視して最新コードでネイティブ推論を実行する**
   既存の推論キャッシュを破棄し、現在のコードで新たに推論結果を生成させます。
   ```bash
   sed -i 's/skip_existing=True/skip_existing=False/g' experiments/issue53_probe_rescue/evaluate_full_rescue_v1.py
   rm -rf logs/issue53_full_eval_rescue_v1
   PYTHONPATH=. .venv_pdf/bin/python experiments/issue53_probe_rescue/evaluate_full_rescue_v1.py > artifacts/step_eval.log 2>&1
   ```

2. **出力された推論結果の評価を確認**
   上記のスクリプトは最後に自動で `re_evaluate_global.py` を呼び出し、ターミナルに評価サマリを出力します。その出力結果の最下部にある `GLOBAL TOTAL` の行が、以下の値と完全に一致していることを確認してください。
   ```bash
   PYTHONPATH=. .venv_pdf/bin/python tools/re_evaluate_global.py --config logs/issue53_full_eval_rescue_v1/eval_config.yaml
   ```
   出力が `GLOBAL TOTAL                        | -     | 3580  | 0     | 1     | 1     | 0     | 100.0% | 100.0%` となることを確認。

これまでの検証では上記の方法により `evaluate_full_rescue_v1.py` を用いた「部分的な推論テスト」が行われ、以下の範囲での検証が完了し、`TP=3580, FP=0, FN=1` の精度が出ることが確認されていました。

*   **検証済みのスコープ (In Scope)**:
    *   **Probe Scan**: 既存のシード(Bands)と画像からの候補枠の新規生成
    *   **Candidate Filtering**: 五線譜・音部記号マスクやヒューリスティクスに基づく候補の事前フィルタリング
    *   **CNN Scoring**: CNNモデルを用いた候補枠のスコアリング
    *   **Box Tightening**: インク情報に基づくトリミング（`trim_box_to_ink`）や NMS（非最大値抑制）
    *   **Evaluation**: 最終候補と正解データ（GT）の Greedy Matching による Recall/Precision 算出
*   **未検証の範囲**:
    ※以下は計算コスト削減のため、事前に生成されたデータ（キャッシュ）を読み込むか、実行をスキップします。
    *   PDFから画像への変換 (`pdf_to_images`)
    *   OMR推論 (`HOMR` などの初期検出)
    *   超解像処理 (`Real-ESRGAN`)
    *   OMR-DLN推論
    *   Hybrid Consensus（各検出結果の統合）
    *   小節番号認識 (`MMR`) と最終的な番号付与

現在、未検証範囲であった「パイプライン前半処理（PDFからの画像変換、OMR推論、Hybrid Consensus等）」を含めた **E2Eパイプライン全体の実行による精度検証** を進めていますが、現在、この前半処理において過去のベースラインと同等の中間生成物（`evaluate_full_rescue_v1.py`で参照されている`scoring_input_eval2_v12` 相当のシード）が生成できておらず、最終的な E2E の精度が維持できていません。以下、このシードをv12と呼ぶ。

## 3. ここまでの調査結果と課題

目標は、E2E パイプライン上で v12 と全く同じ中間結果を出力し、全データセットで「TP=3580, FN=1, FP=0」を再現することです。現在、以下のような課題と差異が判明しています。

*   **二段階アーキテクチャの欠落**: 現在の E2E 実装は1パス化されており、v12 で行われていた「広域なシード生成（Pass 1）」と「厳格なスコアリング（Pass 2）」が混ざってしまっている。
*   **Hybrid Consensus の結合方式**: v12 は各モデル結果を `UNION (論理和)` していたのに対し、現状は `INTERSECTION (論理積)` になっており、初期シード数が極端に少ない。
*   **スケーリングの二重適用バグ**: Pass 2 で高解像度（2x）画像を用いた際、候補座標がダウンスケールされずに保存され、次段で座標が4倍に爆発する問題。
*   **幾何学的フィルターとマスク形状の違い**: これが**最大の障壁**です。v12 では五線譜の「線（ライン）」マスク (`debug_3_staff.png`) が使われていたのに対し、現状の E2E は「領域（リージョン）」マスクを生成・使用しています。このため、五線譜との重なり判定（`filter_by_staff_overlap`）や紙領域判定（`low_paper_overlap`）の挙動が過去と全く異なり、正解候補が CNN スコアリング前後に大量破棄（スコア0.0化）されています。

※ 調査の詳細については `docs/ISSUE120_E2E_SEED_GENERATION_INVESTIGATION.md` を参照してください。実験的な修正を試みましたが、完全な精度復元には至らず、本番コードへの適用はロールバック済みです。

## 4. Next Steps for the Next Session
次回のセッションを担当するAIアシスタントは、以下のプロンプト指示に従って作業を進めてください。

### 【次セッションへの指示プロンプト】
> 本セッションの目標は、E2Eパイプライン上で過去の「v12」と同じ中間生成物（シードと幾何学的フィルタリング結果）が再現できる状態を作り、全データセットでの評価精度 `TP=3580, FN=1, FP=0` を復元することです。
> 
> 前回の調査で、E2E の実装やパラメータ調整だけでは解決せず、「五線譜マスク画像の生成元（線か領域か）」など、根本的な前提条件が v12 生成時と異なっていることが判明しました。したがって、いきなり実装（コードの修正）には入らないでください。
> 
> **タスク1: v12 ベースライン（`scoring_input_eval2_v12` など）の完全な生成方法の調査とドキュメント化**
> 過去のコミット履歴、ドキュメント、およびソースコードを徹底的に調査し、v12 の中間生成物が**過去の時点でどのように作られたのか**を解明してください。
> 
> - どのようなスクリプトが、どのような順序で実行されたか？
> - どのような入力画像、どのような形状のマスク画像（線のマスクか、領域のマスクか）が使われていたか？
> - 具体的なパラメータ設定値はどうなっていたか？
> 
> これらの調査過程（立てた仮説とその実験結果を含む）と、判明した生成方法の全貌をドキュメントにまとめてください。
> 
> **タスク2: パイプライン修正方針の策定（実装はしない）**
> 調査結果を踏まえ、現状の E2E パイプラインをどのように修正すれば、v12 と全く同じ中間生成物が出力される状態を保証できるか、方針を立ててドキュメント化してください。
> もし、過去の仕様への完全な追従が困難（技術的に不可能、あるいは多大な悪影響があるなど）と判断される場合でも、目標精度を下げることはせず、「なぜうまくいかないのか（なぜパイプラインへの組み込みが困難なのか）」の根本理由を詳細に記録してください。