# Issue #13 Action Plan: 実行計画の再構築 (2026-03-03)

現状、Issue #13 (Full Pipeline Phase 2: バッチ最適化・モデル永続化・VRAM管理) の基本的な実装は完了しており、直近の課題であった「ログ過多の抑制」と「HomrのGPU実行修復」も完了しました。

しかし、Issue #13を完全にクローズする前に、紐づくサブイシュー（#24, #26）を別セッションで片付け、クリーンな状態で最終検証へ進む方針に変更します。

## 完了したフェーズ
- **フェーズ 1: パイプライン実行環境の修復とGPU稼働の確証 (完了)**
  - Pythonインタープリタのパス解決修正（`PIPELINE_PYTHON`優先化）。
  - `CUDAExecutionProvider` の稼働確認。
- **フェーズ 2: ログ出力の最適化（ノイズの排除） (完了)**
  - `homr_evaluator.py` の `eprint` をモンキーパッチし、進行ログを `logger.debug` レベルに抑制。
- **フェーズ 3 (一部): Homrのスキップ処理厳格化 (完了)**
  - バッチ内の全ファイル出力が存在する場合のみスキップするよう修正。

---

## 今後のロードマップ（別セッションで順次実行）

### セッション A: 依存関係のクリーンアップ (Issue #26)
- **目的**: パイプラインのコードからハック的なパス解決を排除し、クリーンな構成にする。
- **タスク**:
  - `src/pipeline/main.py` や `detection.py` 内にある `sys.path.insert(0, homr_path)` 等の記述を削除する。
  - `Dockerfile.sr_eval` 等で `PYTHONPATH` に `/workspace/external/homr` 等を含めるよう設定を整理し、環境の再現性を担保する。

### セッション B: MMR（小節番号）ステップのバッチ化 (Issue #24)
- **目的**: 複数ページを処理する際の最大のボトルネック（毎ページRapidOCRとResNet18がロードされる問題）を解消する。
- **タスク**:
  - `tools/generate_numbering_overrides.py` を改修し、1回のサブプロセス呼び出しで複数の画像を一括処理（バッチ化）できるようにする。
  - メモリリークを防ぐため「サブプロセスで実行し、終了時にメモリを解放する」戦略は維持しつつ、ロード回数を1回に抑える。

### セッション C: 最終全体検証とドキュメント化 (Issue #13 クローズへ)
- **目的**: すべてのサブタスクが完了し、クリーンになった状態でパイプラインの安定性とパフォーマンスを証明する。
- **タスク**:
  - 中規模データ（10〜20ページ）でのVRAMストレステスト。RTX 4060 (8GB) 環境でピークVRAMが想定内に収まるか確認。
  - `evaluation2` データセット全体に対してE2E実行。エラー発生時も `--skip-existing` によるリカバリが機能することを検証。
  - パフォーマンス（処理時間やメモリ使用量）を計測し、`docs/ENVIRONMENTS.md` と `docs/ISSUE13_E2E_VERIFICATION_REPORT.md` を最終化する。
