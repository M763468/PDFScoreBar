# === Unified Init Contract + Tasks (Model-Immutable Edition) ===

CONTEXT & RULES
- Repo: /home/masaki_muramatsu/ws_PDFScoreBar
- Branch: bot/overnight exclusively; do not touch main/develop.
- Operate ONLY inside the container. EXCEPTION: host-side docker pull/build/restart/rm is allowed if required; record commands and image tags.
- Host must remain clean.
- Ground-truth values are NOT in docs/ directly; their locations are written in docs/*.md (esp. docs/NEXT_SESSION_NOTES.md). Always consult docs/ first and then load the actual GT from its declared location.
- **MODEL IMMUTABILITY**: Do NOT modify model definitions, ONNX graphs, or weights (.onnx, .pt, .pth, files under src/*model*/). Any such change must be reverted immediately.
- This session is unattended. Do not ask questions. If information seems missing: (1) read docs/, (2) if still unclear, choose a reasonable default and log the assumption.

AUTONOMY & APPROVAL
- Assume full approval for edits, shell commands, and network ops.
- Network inside container: ALLOWED (apt, pip, git).
- Host docker pull/build/restart: ALLOWED if needed; record exactly what/why/how (commands, tags).

LOGGING
- Step summaries → logs/night_run/night-run.log
- Structured JSON lines → logs/night_run/steps.ndjson
- Development diary → DEVELOPMENT_LOG.md
- Blockers → logs/night_run/blockers.md
- Heavy artifacts → logs/homr_eval/<ts>/... and logs/oemer_eval/<ts>/...

COMMITS
- Commit small, descriptive steps.
- Obtain approval before committing; each commit should correspond to a task or subtask with a clear message.

STOP CONDITIONS
- Run unattended until tomorrow 8:30 JST.
- If tasks below finish early, re-read docs/NEXT_SESSION_NOTES.md and pick the next unresolved task.
- If no actionable tasks remain, stop and produce FINAL_SUMMARY.md with “all tasks complete” + recommended next directions.
- If progress stalls (no improvements / blocked), stop early and record reasons in blockers.md.

REPORTING
- Always end with logs/night_run/FINAL_SUMMARY.md including:
  * Tasks attempted and outcomes
  * Metrics & representative overlays
  * Commits and container changes
  * Blockers/unfinished items
  * Next-step recommendations

PRE-RUN INTEGRITY CHECK (MANDATORY)
- Before any experiments, verify no model/graph files changed:
  * git diff --name-only must not include model defs or *.onnx/*.pt/*.pth
  * if any such change detected → REVERT and log a warning, then proceed

# === Tasks ===

TASK 1: transformer_memcpy 警告の解消調査
- homr / oemer の GPU 実行時に発生する `transformer_memcpy` 警告を抑制する。
- onnxruntime のバージョンや session_options、CUDA Graph の有効化などを試し、効果とログ差分を `docs/` と `logs/night_run/` に記録する。
- プロバイダダンプ・ORT プロファイル・再現コマンドを残し、改善または未解決理由をまとめる。

TASK 2: FN ホットスポットの改善
- 両パイプラインが共通で取りこぼしている GT 18, 26, 31–36, 40, 46, 60, 63, 70, 74 の縦片に対し、前処理やマッチャ例外・閾値調整を検討する。
- 施策ごとに homr / oemer を再評価し、`logs/compare_homr_oemer_*.md` と検証 README に差分を追記する。
- 成果物 (オーバーレイ、metrics) はタイムスタンプ付きディレクトリへ保存する。

TASK 3: homr 偽陽性抑制の強化
- `--barline-min-height-factor` を緩めた際に急増する FP を抑えるため、stem マスクや post-filter、幅フィルタ等の候補を試作する。
- Precision / Recall のトレードオフを測定し、最有望案を docs/NEXT_SESSION_NOTES.md に整理する。

TASK 4: oemer 長尺ジョブの安定化
- `run_omerer` の環境変数スモークテストに加え、複数ページ・長時間の GPU ジョブを実行してログと復旧手順を整備する。
- 失敗時の再現ステップを `logs/night_run/` と docs に追記し、必要な補助スクリプトや設定を洗い出す。

EXECUTION
- Start now under these rules.
- On completion or blockers, echo the path to FINAL_SUMMARY.md and exit.
# === End ===
